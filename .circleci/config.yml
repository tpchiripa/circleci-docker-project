version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.11

    steps:
      - checkout

      - run:
          name: Verify pipeline
          command: |
            echo "CircleCI is working!"
            python --version
            git --version
            echo "Build successful"